(defun omu/make-book (title author publisher year pages file)
  "Create a new book and add it to the omu library."
  (interactive "sTitle: \nsAuthor: \nsPublisher: \nsYear: \nsPages: \nsPDF: ")
  (message "Sorry. This isn't really working yet."))

(provide 'omu-make-book)
